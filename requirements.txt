# Python dependencies for PumpIQ
# Install with: pip install -r requirements.txt

# Core Framework
fastapi==0.109.0
uvicorn==0.27.0
gunicorn==21.2.0
pydantic==2.5.3

# HTTP Client
httpx==0.26.0

# AI
google-genai>=1.0.0

# Authentication & Security
python-jose[cryptography]==3.3.0
passlib==1.7.4
python-multipart==0.0.6
bcrypt==4.0.1

# Validation
email-validator==2.1.0
python-dotenv==1.0.1

# Geolocation
digipin==2.0.1

# Cloud Database (Turso — persistent SQLite on Vercel)
libsql-client>=0.3.0

# Blockchain (Base L2 / Ethereum) — optional, lazily imported
# web3>=6.0.0     # Excluded from Vercel (heavy native deps); install locally
# py-solc-x>=2.0.0
