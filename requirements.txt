# Python dependencies for PumpIQ
# Install with: pip install -r requirements.txt

# Core Framework
fastapi==0.109.0
uvicorn==0.27.0
pydantic==2.5.3

# HTTP Client
httpx==0.26.0

# AI
google-genai>=1.0.0

# Authentication & Security
python-jose[cryptography]==3.3.0
passlib==1.7.4
python-multipart==0.0.6
bcrypt==4.0.1

# Validation
email-validator==2.1.0
python-dotenv==1.0.1

# Note: Removed gunicorn (not needed for Vercel serverless)
# Note: Removed Celery/Redis (incompatible with serverless - use Vercel Cron Jobs instead)
