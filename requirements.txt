# Python dependencies for NexYpher
# Install with: pip install -r requirements.txt

# Core Framework
fastapi==0.109.0
uvicorn==0.27.0
gunicorn==21.2.0
pydantic==2.5.3

# HTTP Client
httpx==0.26.0

# AI
google-genai>=1.0.0

# Database (Supabase)
supabase>=2.0.0

# Authentication & Security
python-jose[cryptography]==3.3.0
passlib==1.7.4
python-multipart==0.0.6
bcrypt==4.0.1

# Validation
email-validator==2.1.0
python-dotenv==1.0.1

# Geolocation
digipin==2.0.1

# Cloud Database (Turso — persistent SQLite on Vercel)
libsql-client>=0.3.0

# Google Cloud Storage (persistent SQLite backup on Vercel)
google-cloud-storage>=2.14.0

# Blockchain (Base L2 / Ethereum) — optional, lazily imported
web3>=6.0.0
# py-solc-x>=2.0.0  # Only needed for contract compilation/deployment

# ML / AI Algorithms — optional, lazily imported with graceful degradation
# Heavy packages excluded from Vercel (exceed 500 MB Lambda limit).
# Install locally:  pip install -r requirements-ml.txt
# xgboost>=2.0.0
# scikit-learn>=1.3.0
# optuna>=3.4.0
# pandas-ta>=0.3.14b1

# Deep Learning — optional, lazily imported (heavy; skip on Vercel)
# tensorflow>=2.15.0   # Install locally: pip install tensorflow
